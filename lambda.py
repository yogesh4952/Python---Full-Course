#Same as function
#Shorter way to define function that calculate the single expression
#can use directly in an expression


def fy(x,y):
    return x+y;

f= lambda x,y:x+y;

#Here in above example both are same
print(f)